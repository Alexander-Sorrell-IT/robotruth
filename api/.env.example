# Set these on your API host (Railway / Render / Fly).
REDIS_URL=          # Upstash Redis URL (rediss://...) — enables persistent receipts
GITHUB_TOKEN=       # GitHub PAT with public_repo read — raises rate limit to 5000/hr
DIFF_MAX_BYTES=1000000
